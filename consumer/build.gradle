apply plugin: 'com.github.jk1.tcdeps'
apply plugin: 'java'

version = '1.0'

configurations {
    conf
}

teamcity{
    url = 'http://teamcity.jetbrains.com'
}

dependencies {
    conf tc('bt345',"0.8.1429",'kotlin-compiler-0.8.1429.zip!/kotlinc/build.txt')
    conf tc('bt345',"0.8.1429",'kotlin-compiler-0.8.1429.zip')
}

buildscript{
    dependencies {
        classpath fileTree(dir: '../build/libs', include: '*.jar')
    }
}

task test1 << {
    configurations.conf.each {
        conf -> println(conf)
    }
}

